# Action-Model-Desktop

This Action Model uses the power of LLMs and VLMs to generate workflows on the go. The user will be able to run and edit workflows as they like.

To run the above repo:

Clone the repo in your working directory or download the Zip folder and extract it in your working directory.

Open Powershell/cmd

Run the Task_Decomposer.py file
